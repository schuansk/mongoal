import { DefaultTheme } from 'styled-components';

import { colors } from './colors';
import { constants } from './constants';

const stylertTheme: DefaultTheme = {
  colors,
  constants,
};

export default stylertTheme;
