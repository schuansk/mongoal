import AccountModel from './accountModel';
import CategoryModel from './categoryModel';
import TransactionModel from './transactionModel';

export const models = [AccountModel, CategoryModel, TransactionModel];
